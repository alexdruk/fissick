{
  "name": "fossick-tests",
  "version": "1.0.0",
  "scripts": {
    "test": "playwright test --config=playwright.config.js",
    "test:smoke": "playwright test --config=playwright.config.js tests/smoke.spec.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
